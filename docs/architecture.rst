Architecture
============

High-Level Components
---------------------

- Flask monolith for web routes, templates, API endpoints, and admin tools.
- RQ worker for background work and periodic jobs.
- Vite/React frontend (dev profile).
- Postgres + PgBouncer for persistent storage.
- Redis for caching/rate limiting/queue broker.
- Nginx as reverse proxy.

Project Layout
--------------

- ``backend/``: Flask app, services, models, templates, and microservices.
- ``frontend/``: React SPA sources and build tooling.
- ``infra/``: Docker and infrastructure configuration.
- ``tests/``: Backend and UI automated tests.

Integrations
------------

- Scryfall data and symbols cache
- Commander Spellbook combo sync
- MTGJSON pricing service
- EDHREC recommendation/metadata service

Upgrade Roadmap
---------------

- Treat the Flask ``/api`` and Django ``/api-next`` folder surfaces as one contract. Shared folder/card payload builders now live in ``backend/shared/api/folder_payloads.py`` and should remain the source of truth for future migration work.
- Keep folder visibility rules centralized. Ownership, public folders, direct shares, and friend access should be implemented once and reused by both framework surfaces before expanding Django coverage.
- Shrink the largest mixed-responsibility modules next. ``backend/core/routes/base.py`` now mostly re-exports MTG and folder helpers from ``backend/shared/mtg.py`` plus shared wishlist/database helpers, with print lookup, card-face parsing, color-identity, image, and token helper logic extracted into ``backend/shared/mtg_prints.py``. Wishlist route logic now lives in ``backend/core/domains/decks/services/wishlist_service.py`` with display/source-folder enrichment in ``backend/core/domains/decks/services/wishlist_display_service.py`` and row normalization/upsert helpers in ``backend/core/domains/decks/services/wishlist_mutation_service.py``, deck gallery wrappers now live in ``backend/core/domains/decks/services/deck_gallery_service.py`` with drawer payload assembly in ``backend/core/domains/decks/services/deck_gallery_drawer_service.py``, overview-page context assembly in ``backend/core/domains/decks/services/deck_gallery_overview_service.py``, and shared commander-thumbnail/owner-summary helpers in ``backend/core/domains/decks/services/deck_gallery_shared_service.py``. Build-session POST/session-management and EDHREC refresh flows now live in ``backend/core/domains/decks/services/build_session_mutation_service.py``, while page-context and drawer-summary assembly now live in ``backend/core/domains/decks/services/build_session_page_context_service.py``, leaving ``backend/core/domains/decks/services/build_session_service.py`` focused on render-time build analysis helpers and compatibility wrappers. Deck token overview logic now lives in ``backend/core/domains/decks/services/deck_tokens_service.py``, opening-hand route/state flow now lives in ``backend/core/domains/decks/services/opening_hand_service.py``, opening-hand state serialization and deck expansion now live in ``backend/core/domains/decks/services/opening_hand_state_service.py``, opening-hand deck loading now lives in ``backend/core/domains/decks/services/opening_hand_deck_source_service.py``, opening-hand gameplay state transitions now live in ``backend/core/domains/decks/services/opening_hand_gameplay_service.py``, and opening-hand preview/token payload lookup assembly now lives in ``backend/core/domains/decks/services/opening_hand_lookup_service.py`` plus ``backend/core/domains/decks/services/opening_hand_payload_service.py``. Proxy deck creation now lives in ``backend/core/domains/decks/services/proxy_deck_service.py``, deck-from-collection flow now lives in ``backend/core/domains/decks/services/deck_from_collection_service.py``, commander bracket reference and ruleset data now live in ``backend/core/domains/decks/services/commander_bracket_reference_service.py``, Commander Spellbook dataset loading/matching now lives in ``backend/core/domains/decks/services/commander_spellbook_service.py``, commander bracket evaluation now lives in ``backend/core/domains/decks/services/commander_bracket_evaluation_service.py``, and commander bracket card models, print resolution, and classifier helpers now live in ``backend/core/domains/decks/services/commander_bracket_card_service.py`` with ``backend/core/domains/decks/services/commander_brackets.py`` reduced to the compatibility wrapper. Commander bracket reference and Spellbook directory views now live in ``backend/core/domains/decks/services/commander_info_service.py``, commander assignment endpoints now live in ``backend/core/domains/decks/services/commander_assignment_service.py``, deck-to-build-session transfer now lives in ``backend/core/domains/decks/services/send_to_build_service.py``, folder detail and shared-folder rendering now live in ``backend/core/domains/decks/services/folder_detail_service.py``, row/token/type analysis for that page now lives in ``backend/core/domains/decks/services/folder_detail_analysis_service.py``, commander-media/bracket/EDHREC enrichment for that page now lives in ``backend/core/domains/decks/services/folder_detail_commander_context_service.py``, raw card loading/print resolution/sorting for that page now lives in ``backend/core/domains/decks/services/folder_detail_card_state_service.py``, card VM/group/curve assembly for that page now lives in ``backend/core/domains/decks/services/folder_detail_card_presentation_service.py``, cards-context composition for that page now lives in ``backend/core/domains/decks/services/folder_detail_cards_context_service.py``, folder shell/move-target assembly for that page now lives in ``backend/core/domains/decks/services/folder_detail_folder_shell_service.py``, and the remaining page-context orchestration now lives in ``backend/core/domains/decks/services/folder_detail_page_context_service.py``. Folder metadata/count mutations now live in ``backend/core/domains/decks/services/folder_metadata_service.py``, folder sharing management now lives in ``backend/core/domains/decks/services/folder_sharing_service.py``, collection route rendering now lives in ``backend/core/domains/cards/services/collection_service.py``, collection request parsing now lives in ``backend/core/domains/cards/services/collection_request_service.py``, collection query/filter/sort planning now lives in ``backend/core/domains/cards/services/collection_query_service.py``, collection card list presentation now lives in ``backend/core/domains/cards/services/collection_card_list_view_service.py``, collection overview now lives in ``backend/core/domains/cards/services/collection_overview_service.py``, card-browser facets and rarity/set options now live in ``backend/core/domains/cards/services/collection_facets_service.py``, list-checker orchestration now lives in ``backend/core/domains/cards/services/list_checker_service.py`` with collection/folder inventory lookup and face-rescue logic in ``backend/core/domains/cards/services/list_checker_inventory_service.py``, parsing/face matching in ``backend/core/domains/cards/services/list_checker_parsing_service.py``, Scryfall metadata enrichment in ``backend/core/domains/cards/services/list_checker_scryfall_service.py``, folder-label loading and breakdown formatting in ``backend/core/domains/cards/services/list_checker_result_service.py``, row assembly in ``backend/core/domains/cards/services/list_checker_result_builder_service.py``, and CSV export formatting in ``backend/core/domains/cards/services/list_checker_export_service.py``. Scryfall browser rendering now lives in ``backend/core/domains/cards/services/scryfall_browser_service.py``, Scryfall print-detail/API rendering now lives in ``backend/core/domains/cards/services/scryfall_print_service.py``, Scryfall set overview/gallery rendering now lives in ``backend/core/domains/cards/services/scryfall_sets_service.py``, shared Scryfall view helpers now live in ``backend/core/domains/cards/services/scryfall_shared_service.py``, Scryfall runtime/session/path/live-fetch helpers now live in ``backend/core/domains/cards/services/scryfall_runtime_service.py``, set-name/release/code normalization helpers now live in ``backend/core/domains/cards/services/scryfall_set_metadata_service.py``, local default-cards JSON catalog search/token resolution now lives in ``backend/core/domains/cards/services/scryfall_catalog_service.py``, print label/image bundle helpers now live in ``backend/core/domains/cards/services/scryfall_print_summary_service.py``, and set profile/image sampling now lives in ``backend/core/domains/cards/services/scryfall_set_profile_service.py``. CSV import queueing/status/template flows now live in ``backend/core/domains/cards/services/csv_import_flow_service.py``, CSV file/header/table parsing now lives in ``backend/core/domains/cards/services/csv_import_file_service.py``, background CSV import enqueue/inline/job orchestration now lives in ``backend/shared/jobs/csv_import_jobs_service.py``, card exports now live in ``backend/core/domains/cards/services/card_export_service.py``, manual list import now lives in ``backend/core/domains/cards/services/manual_import_service.py``, and folder-category updates for import followups now live in ``backend/core/domains/cards/services/import_category_service.py`` with shared parsing/upload helpers in ``backend/core/domains/cards/services/import_shared_service.py``. Owned card detail / quick-view logic now lives in ``backend/core/domains/cards/services/card_detail_service.py``, shared-folder and friendship UI logic now lives in ``backend/core/domains/cards/services/shared_folders_service.py``, card bulk-move/delete and printing-update logic now lives in ``backend/core/domains/cards/services/card_mutation_service.py``, and the user dashboard now lives in ``backend/core/domains/users/services/dashboard_service.py``. Folder-name helpers are shared from ``backend/shared/folders.py``, game landing/log/admin/import-export routes now live in ``backend/core/domains/games/services/game_overview_service.py``, game roster/pod page-context assembly now lives in ``backend/core/domains/games/services/game_players_payload_service.py``, game roster/pod POST mutations now live in ``backend/core/domains/games/services/game_players_action_service.py``, the route wrapper stays in ``backend/core/domains/games/services/game_players_service.py``, game metrics overview/player/pod routes now live in ``backend/core/domains/games/services/game_metrics_service.py``, user/deck/public-dashboard metrics breakdown pages now live in ``backend/core/domains/games/services/game_metrics_breakdown_service.py``, the remaining games compatibility barrel now lives in ``backend/core/domains/games/services/game_compat_service.py``, and ``backend/core/domains/games/services/game_service.py`` is reduced to the route wrapper plus compatibility delegation. Game metrics date/filter/scope plumbing now lives in ``backend/core/domains/games/services/game_metrics_support_service.py``, game metrics aggregation/query helpers now live in ``backend/core/domains/games/services/game_metrics_query_service.py``, shared game session parsing/deck lookup/view payload helpers now live in ``backend/core/domains/games/services/game_session_shared_service.py``, and game session detail, form context/rendering, request parsing, and persistence now live in ``backend/core/domains/games/services/game_session_form_context_service.py``, ``backend/core/domains/games/services/game_session_form_parsing_service.py``, and ``backend/core/domains/games/services/game_session_form_mutation_service.py`` with ``backend/core/domains/games/services/game_session_form_service.py`` reduced to the route-facing wrapper. EDHREC cache refresh/write orchestration now lives in ``backend/core/domains/decks/services/edhrec_cache_refresh_service.py`` with ``backend/core/domains/decks/services/edhrec_cache_service.py`` reduced to snapshot/query wrappers over the cache tables. Admin folder/category management now lives in ``backend/core/services/admin_folder_categories_service.py``, admin system-health/data-ops logic now lives in ``backend/core/services/admin_system_service.py``, admin oracle/card-role pages now live in ``backend/core/services/admin_oracle_tags_service.py``, admin game-deck mapping now lives in ``backend/core/services/admin_game_mapping_service.py``, admin request/job-status handling now lives in ``backend/core/services/admin_requests_service.py``, admin user management now lives in ``backend/core/services/admin_user_management_service.py``, admin console actions now live in ``backend/core/services/admin_console_service.py``, and the old cards-service proxy-deck wrappers have been removed from the live codebase.
- Keep startup/schema recovery and local Scryfall catalog browsing isolated from route bootstrap. Runtime SQLite validation and legacy schema repair now live in ``backend/shared/database/schema_bootstrap.py``, request logging and static-asset CSP extension now live in ``backend/shared/app_runtime.py``, and CLI command registration now lives in ``backend/shared/app_cli.py`` with command groups split across ``backend/shared/app_cli_general.py``, ``backend/shared/app_cli_scryfall.py``, and ``backend/shared/app_cli_users.py`` so ``backend/app.py`` stays focused on factory/runtime orchestration. Local default-cards JSON search/token helpers now live in ``backend/core/domains/cards/services/scryfall_catalog_service.py``. Print-summary helpers now live in ``backend/core/domains/cards/services/scryfall_print_summary_service.py``, default-cards name/index lookup helpers now live in ``backend/core/domains/cards/services/scryfall_index_service.py``, cached print metadata and local browser filtering now live in ``backend/core/domains/cards/services/scryfall_metadata_service.py``, HTTP session and bulk-download wrappers now live in ``backend/core/domains/cards/services/scryfall_http_service.py``, in-memory cache bootstrap/reset/load helpers now live in ``backend/core/domains/cards/services/scryfall_cache_state_service.py``, local rulings bulk loading/indexing now lives in ``backend/core/domains/cards/services/scryfall_rulings_service.py``, and set-profile/image-sample helpers now live in ``backend/core/domains/cards/services/scryfall_set_profile_service.py``, leaving ``backend/core/domains/cards/services/scryfall_cache.py`` focused on cache state, indexing, and compatibility wrappers.
- Keep EDHREC and oracle recompute modules split between orchestration and parsing/query helpers. EDHREC commander target discovery now lives in ``backend/core/domains/decks/services/edhrec/edhrec_target_service.py``, EDHREC payload/cardlist/type parsing now lives in ``backend/core/domains/decks/services/edhrec/edhrec_payload_service.py``, EDHREC HTTP/payload assembly and shared commander-bundle fetch now live in ``backend/core/domains/decks/services/edhrec/edhrec_ingestion_fetch_service.py``, commander-tag refresh flow now lives in ``backend/core/domains/decks/services/edhrec/edhrec_tag_refresh_service.py``, EDHREC schema/write/finalize helpers now live in ``backend/core/domains/decks/services/edhrec/edhrec_ingestion_persistence_service.py``, local EDHREC cache target discovery/tag extraction now lives in ``backend/core/domains/decks/services/edhrec_cache_target_service.py``, local EDHREC cache query/grouping helpers now live in ``backend/core/domains/decks/services/edhrec_cache_query_service.py``, shared oracle print analysis now lives in ``backend/shared/jobs/background/oracle_profile_service.py``, deck-tag synergy scoring now lives in ``backend/shared/jobs/background/oracle_deck_tag_synergy_service.py``, and card role/subrole rebuilds now live in ``backend/shared/jobs/background/oracle_role_recompute_service.py``. The remaining ingestion/cache/recompute modules should stay focused on persistence and job orchestration.
- Preserve deterministic frontend installs. The frontend now uses a committed ``package-lock.json`` with ``npm ci`` in local automation, CI, and container builds; dependency updates should refresh the lockfile in the same change.
- Add production-shaped verification as migration work expands. SQLite-backed tests are still useful for speed, but API and query refactors should also gain at least a small PostgreSQL-backed smoke path in CI.
